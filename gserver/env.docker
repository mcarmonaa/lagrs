# ENVIRONMENT VARIABLES TO CONFIG GSERVER
GSERVER_ADDR=localhost:8080
GSERVER_HOST=localhost
GSERVER_PORT=8080

# ENVIRONMENT VARIABLES TO CONNECT TO SERVICEUSERS
SERVICEUSERS_ADDR=localhost:8081
SERVICEUSERS_HOST=localhost
SERVICEUSERS_PORT=8081

# [manuel@manubook gserver]$ docker run --rm -p 8080:8080 --name gserver_1 --network host --env-file ./env.docker gserver